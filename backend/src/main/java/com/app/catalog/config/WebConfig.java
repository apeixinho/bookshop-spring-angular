package com.app.catalog.config;

import org.springframework.context.annotation.Configuration;

/** Intentionally empty — CORS is configured once in {@link SecurityConfig}. */
@Configuration
public class WebConfig {
}
